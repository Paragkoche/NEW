import { FileInterceptorInterceptor } from './file-interceptor.interceptor';

describe('FileInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new FileInterceptorInterceptor()).toBeDefined();
  });
});
